package fortree;

import java.util.PriorityQueue;
import java.util.Queue;

/*
 * Binary Heap : Max-Heap (Parent > Child)
 * Node ở vị trí thứ i -> con trái 2i + 1, con phải 2i + 2 (Cài đặt = mảng)
 */

public class HeapSort {
    static int[] array = { 10, 23, 3, 0, 102, 34, 89, 54, 67, 903, 102 };

    void sort(int[] array) {
        Queue<Integer> q = new PriorityQueue<>();
    }
}
